/* pages/script-list/script-list.wxss */
.container {
  min-height: 100vh;
  background: #F5F7FA;
}

/* 自定义导航栏 */
.nav-bar {
  background: linear-gradient(135deg, #4A90D9, #4096FF);
  padding-top: var(--status-bar-height);
  padding-bottom: 24rpx;
}

.nav-bar-content {
  padding: 20rpx 32rpx 0;
}

.nav-title {
  display: block;
  font-size: var(--font-3xl);
  font-weight: 700;
  color: var(--text-inverse);
  margin-bottom: 8rpx;
}

.nav-subtitle {
  font-size: var(--font-base);
  color: rgba(255, 255, 255, 0.8);
}

/* 搜索框 */
.search-section {
  padding: 24rpx 32rpx;
  background: var(--bg-card);
}

.search-box {
  display: flex;
  align-items: center;
  background: #F5F7FA;
  border-radius: 40rpx;
  padding: 16rpx 28rpx;
}

.search-icon {
  font-size: var(--font-normal);
  margin-right: 12rpx;
}

.search-input {
  flex: 1;
  font-size: var(--font-normal);
  color: #333333;
}

.search-placeholder {
  color: var(--text-tertiary);
  font-size: var(--font-md);
}

.search-clear {
  font-size: var(--font-normal);
  color: var(--text-tertiary);
  padding: 8rpx;
}

/* 话术列表 */
.script-list {
  padding: 24rpx 32rpx;
}

.script-card {
  background: var(--bg-card);
  border-radius: 20rpx;
  margin-bottom: 24rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
  transition: all 0.2s ease;
}

.script-card-hover {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
}

/* 卡片头部 */
.card-header {
  display: flex;
  align-items: center;
  padding: 28rpx 28rpx 20rpx;
}

.script-icon {
  width: 72rpx;
  height: 72rpx;
  --icon-bg: linear-gradient(135deg, var(--success), #73D13D);
  --icon-color: var(--text-inverse);
  border-radius: 16rpx;
  margin-right: 20rpx;
  flex-shrink: 0;
}

.script-info {
  flex: 1;
  min-width: 0;
}

.script-name {
  display: block;
  font-size: var(--font-lg);
  font-weight: 600;
  color: #1A1B1C;
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.script-tags {
  display: flex;
  gap: 12rpx;
}

.script-tag {
  font-size: var(--font-xs);
  color: var(--success);
  background: #F6FFED;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.favorite-btn {
  padding: 12rpx;
  flex-shrink: 0;
}

.favorite-icon {
  font-size: var(--font-3xl);
  color: #D9D9D9;
}

.favorite-btn.favorited .favorite-icon {
  color: var(--warning);
}

/* 卡片主体 */
.card-body {
  padding: 0 28rpx 16rpx;
}

.applicable-label {
  font-size: var(--font-base);
  color: #8C8C8C;
  font-weight: 500;
}

.applicable-text {
  font-size: var(--font-base);
  color: #595959;
  line-height: 1.6;
}

/* 话术预览 */
.card-preview {
  padding: 0 28rpx 20rpx;
}

.preview-text {
  font-size: var(--font-base);
  color: #8C8C8C;
  line-height: 1.6;
  background: #FAFAFA;
  padding: 16rpx 20rpx;
  border-radius: 12rpx;
  display: block;
  border-left: 4rpx solid var(--success);
}

/* 卡片底部 */
.card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 28rpx;
  border-top: 1rpx solid #F0F0F0;
  background: #FAFAFA;
}

.footer-hint {
  font-size: var(--font-base);
  color: #4A90D9;
}

.copy-btn {
  background: linear-gradient(135deg, var(--success), #73D13D);
  padding: 12rpx 28rpx;
  border-radius: 24rpx;
}

.copy-btn-text {
  font-size: var(--font-base);
  color: var(--text-inverse);
  font-weight: 500;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 48rpx;
}

.empty-icon {
  font-size: 96rpx;
  margin-bottom: 24rpx;
  opacity: 0.5;
}

.empty-title {
  font-size: var(--font-lg);
  color: var(--text-secondary);
  font-weight: 600;
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: var(--font-base);
  color: var(--text-tertiary);
  text-align: center;
  margin-bottom: 40rpx;
}

.empty-btn {
  background: linear-gradient(135deg, #4A90D9, #4096FF);
  color: var(--text-inverse);
  padding: 20rpx 48rpx;
  border-radius: 32rpx;
  font-size: var(--font-normal);
  font-weight: 600;
}
