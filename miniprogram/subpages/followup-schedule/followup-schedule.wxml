<!--subsubsubpages/followup-schedule/followup-schedule.wxml-->
<view class="container">
  <!-- 渐变头部 -->
  <view class="gradient-header">
    <view class="header-content">
      <view class="header-top">
        <view class="header-icon app-icon app-icon-hover icon-clock" style="width:56rpx;height:56rpx;"></view>
        <view class="header-title">投诉跟进时间表</view>
      </view>
      <view class="header-desc">法定时限+升级路径，掌握投诉节奏</view>
    </view>
  </view>

  <!-- 内容区 -->
  <view class="content">
    <!-- 时间线 -->
    <view class="timeline">
      <view
        class="timeline-item"
        wx:for="{{list}}"
        wx:key="id"
        data-index="{{index}}"
        bindtap="onItemTap"
        hover-class="timeline-item-hover"
      >
        <view class="timeline-dot">
          <view class="dot-inner" style="background: {{item.color || '#06B6D4'}};"></view>
        </view>
        <view class="timeline-line" wx:if="{{index < list.length - 1}}"></view>
        <view class="timeline-content">
          <view class="timeline-header">
            <view class="timeline-header-left">
              <text class="timeline-time">{{item.time_node}}</text>
              <text class="timeline-title" wx:if="{{item.time_display}}">{{item.time_display}}</text>
            </view>
            <view class="timeline-arrow app-icon app-icon-default icon-chevron-down {{expandedIndex === index ? 'expanded' : ''}}" style="width:32rpx;height:32rpx;--icon-bg:transparent;--icon-color:#999;"></view>
          </view>
          <view class="timeline-detail {{expandedIndex === index ? 'show' : ''}}">
            <view class="timeline-desc" wx:if="{{item.legal_basis && item.legal_basis !== '—'}}">{{item.legal_basis}}</view>
            <view class="timeline-action" wx:if="{{item.action}}">
              <text class="action-label">建议动作：</text>
              <text class="action-text">{{item.action}}</text>
            </view>
            <view class="timeline-escalate" wx:if="{{item.escalate_to && item.escalate_to !== '—'}}">
              <text class="escalate-label">升级路径：</text>
              <text class="escalate-text">{{item.escalate_to}}</text>
            </view>
            <view class="timeline-extra" wx:if="{{item.detail}}">
              <text class="extra-text">{{item.detail}}</text>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- 提示 -->
    <view class="tip-card">
      <view class="tip-icon app-icon app-icon-default icon-info" style="width:32rpx;height:32rpx;margin-right:12rpx;flex-shrink:0;"></view>
      <view class="tip-text">以上时限为一般规定，具体以各监管部门实际处理流程为准。如遇超期未处理，可向上级部门投诉或申请行政复议。保留好所有沟通记录和证据。</view>
    </view>

    <view style="height: 40rpx;"></view>
  </view>
</view>
